- name: Hello World
  tasks:
  - name: Hello Message
    debug:
      msg: "Hello World!"
  - name: Collect info
    command: "echo $HOSTNAME"
    register: info
  - name: Output info
    debug:
      msg: "{{ info.stdout }}"
